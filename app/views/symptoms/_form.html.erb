<%= form_for ([@crop,@symptom]), html: { multipart: true } do |f| %>
  <% if @symptom.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@symptom.errors.count, "error") %> prohibited this crop from being saved:</h2>
    <ul>
    <% @symptom.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <div class="form-group">
    <div><%= f.label "Name" %><br>
    <%= f.text_field :title, class: "form-control"%></div>
  </div>
      <%= f.label :image %><br>
      <%= f.file_field :image%>
  <div class="form-group">
    <div><%= f.label "Symptom" %><br>
    <%= f.text_area :text, class: "form-control"%></div>
  </div>
  <div class="form-group">
    <div><%= f.label "Cause" %><br>
    <%= f.text_area :cause, class: "form-control"%></div>
  </div>
  <div class="form-group">
    <div><%= f.label "Remedy" %><br>
    <%= f.text_area :remedy, class: "form-control"%></div>
  </div>
 
  <%= button_tag(type: 'submit', class: "btn btn-success") do %>
    Submit
  <% end %>
<% end %>