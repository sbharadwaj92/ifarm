<%= form_for ([@crop,@development]), html: { multipart: true } do |f| %>
  <% if @development.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@development.errors.count, "error") %> prohibited this crop from being saved:</h2>
    <ul>
    <% @development.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <div class="form-group">
    <div><%= f.label :title %><br>
    <%= f.text_field :title, class: "form-control"%></div>
  </div>
      <%= f.label :image %><br>
      <%= f.file_field :image%>
  <div class="form-group">
    <div><%= f.label :text %><br>
    <%= f.text_area :text, class: "form-control"%></div>
  </div>
 
  <%= button_tag(type: 'submit', class: "btn btn-success") do %>
    Submit
  <% end %>
<% end %>